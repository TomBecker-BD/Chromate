namespace TestingUtilities
{
    /// <summary>
    /// Info levels correspond to log levels. 
    /// </summary>
    public enum InfoLevel
    {
        Trace = 0, Debug, Info, Warn, Error, Fatal
    };
}
